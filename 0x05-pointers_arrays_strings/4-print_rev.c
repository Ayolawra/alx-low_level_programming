#include "main.h"
#include <stdio.h>

/**
 * print_rev - print string in reverse
 *
 * @s: string to act on
 *
 * Return: void
 */
void print_rev(char *s)
{
	int i;

	while (str[i])
	{
		putchar(str[i__]);
	}
	putchar('\n');
}
